 add codemagic.yaml
 commit -m 'Add first workflow'
 push
workflows:
  default-workflow:
    name: Build Workflow
    max_build_duration: 60
    instance_type: mac_mini_m1
    scripts:
      - name: Get dependencies
        script: | 
          echo "Building Project Flash..."
